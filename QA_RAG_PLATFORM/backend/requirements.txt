# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Config
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Database
sqlmodel>=0.0.21
aiosqlite>=0.20.0

# Parsers
pymupdf>=1.24.0
pandas>=2.2.0
openpyxl>=3.1.0
xlrd>=2.0.1
python-docx>=1.1.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
markdown>=3.7
pyyaml>=6.0.0

# Embeddings
mistralai>=1.2.0,<2.0.0

# Vector DB
pinecone>=5.0.0

# Hybrid search
rank-bm25>=0.2.2

# Multi-agent orchestration
langgraph>=0.2.0

# Full-text search (OpenSearch — optional, replaces in-memory BM25 when OPENSEARCH_URL is set)
opensearch-py>=2.4.0

# Observability (OpenTelemetry — traces agent runs, FastAPI requests, search calls)
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0

# LLM — primary + router fallbacks
groq>=0.13.0
openai>=1.50.0    # used as the OpenAI-compat client for Groq, Mistral, Gemini, OpenAI

# Reranker (optional)
cohere>=5.0.0

# V3 — AST parsing
tree-sitter>=0.21.0,<0.22.0
tree-sitter-languages>=1.10.0

# Utils
aiofiles>=24.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
numpy>=1.26.0

# PostgreSQL (optional — set DATABASE_URL=postgresql://... to activate)
psycopg2-binary>=2.9.0

# Redis cache + pub/sub (optional — set REDIS_URL=redis://... to activate)
redis>=5.0.0

# OAuth2 / SSO
httpx>=0.27.0

# Neo4j Knowledge Graph (optional — set NEO4J_URI to activate)
neo4j>=5.15.0

# Celery async task queue (optional — set CELERY_BROKER_URL or REDIS_URL)
# celery[redis]>=5.3.0

# ClamAV virus scanning (optional — requires clamd daemon or clamscan binary)
# Install: pip install pyclamd
# Docker:  docker run -d -p 3310:3310 clamav/clamav:stable
# pyclamd>=0.4.0

# SAML 2.0 SSO (optional — requires libxml2-dev libxmlsec1-dev OS packages)
# Install: pip install python3-saml
# Skip in dev if not needed — SAML endpoints gracefully 503 when absent
# python3-saml>=3.0.0

# HashiCorp Vault (optional — set VAULT_ADDR to activate)
# pip install hvac
# hvac>=2.1.0

# OpenSearch vector store (optional — set VECTOR_STORE=opensearch to activate)
# pip install opensearch-py
# opensearch-py>=2.4.0
